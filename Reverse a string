/*India is my country
aidni si ym yrtnuoc*/

#include <iostream>
#include<bits/stdc++.h>
using namespace std;

void strrev(string &s)
{
    int n=s.length();
    for(int i=s.length()-1;i>=0;i--)
    {
        cout<<s[i];
    }
    
}
int main()
{
string s;
cin>>s;
strrev(s);
    return 0;
}
